# Published experiment artifacts

This directory contains selected raw tables, configs, logs, and reports. It intentionally excludes model checkpoints, virtual environments, caches, and the full local run history. See `EXPERIMENT_SNAPSHOT.md` and `NEXT_PLAN.md` for interpretation and caveats.
